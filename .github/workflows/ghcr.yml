name: Build and Push GHCR

on:
  push:
    branches: ["main"]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  docker:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: prod
            dockerfile: docker/Dockerfile
            latest_tag: latest
            sha_prefix: sha-
            tag_suffix: ""
            image_title: "Meow Bot Runtime"
            image_description: "Lightweight runtime image for Meow Whatsmeow bot with ffmpeg/webp and terminal tools."
          - name: dev
            dockerfile: docker/Dockerfile.dev
            latest_tag: dev-latest
            sha_prefix: dev-sha-
            tag_suffix: -dev
            image_title: "Meow Bot Devbox"
            image_description: "Development image for Meow Whatsmeow bot with full Go toolchain, ffmpeg/webp, and terminal tools."
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute image tags
        id: tags
        shell: bash
        run: |
          set -euo pipefail
          image="ghcr.io/${{ github.repository }}"
          sha_short="${GITHUB_SHA::12}"
          tags=("${image}:${{ matrix.sha_prefix }}${sha_short}")

          if [[ "${GITHUB_REF}" == "refs/heads/main" ]]; then
            tags+=("${image}:${{ matrix.latest_tag }}")
          fi

          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            version="${GITHUB_REF#refs/tags/}"
            tags+=("${image}:${version}${{ matrix.tag_suffix }}")
          fi

          {
            echo "tags<<EOF"
            printf '%s\n' "${tags[@]}"
            echo "EOF"
          } >> "${GITHUB_OUTPUT}"

      - name: Build and push (${{ matrix.name }})
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.tags.outputs.tags }}
          labels: |
            org.opencontainers.image.title=${{ matrix.image_title }}
            org.opencontainers.image.description=${{ matrix.image_description }}
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.documentation=https://github.com/${{ github.repository }}/tree/main/docker
